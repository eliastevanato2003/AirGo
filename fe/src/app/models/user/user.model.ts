export interface User {
    Admin: null,
    Cognome: string,
    Dob: null,
    Email: string,
    IdEmail: null,
    IdUtente: null,
    IsActive: null,
    Mail: null,
    Nome: string,
    Password: string,
    Telefono: string
}

export interface userProfile {
    name: string;
    surname: string;
    password: string;
    email: string;
    phone: string;
}